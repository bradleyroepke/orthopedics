# Database (PostgreSQL - e.g., Supabase, Neon, or local)
DATABASE_URL="postgresql://user:password@host:5432/database"

# NextAuth - Generate a secure random string (32+ characters)
# You can generate one with: openssl rand -base64 32
AUTH_SECRET="generate-a-secure-random-string-at-least-32-chars"

# Documents folder path - Path to your Orthopedics PDF folder
DOCUMENTS_PATH="/path/to/your/orthopedics/folder"

# Admin credentials for initial setup (used by npm run db:seed)
ADMIN_EMAIL="your-email@example.com"
ADMIN_PASSWORD="choose-a-secure-password"
